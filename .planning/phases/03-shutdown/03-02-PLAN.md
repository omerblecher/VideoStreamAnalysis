---
phase: 03-shutdown
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified: []
autonomous: false
requirements:
  - SHUT-01
  - SHUT-02
  - SHUT-03

must_haves:
  truths:
    - "After the last frame is played, all three processes exit without manual intervention"
    - "No zombie processes remain after the pipeline exits"
    - "Codebase is committed, tagged phase-c, and pushed to GitHub"
  artifacts:
    - path: "main.py"
      provides: "Shutdown-capable entry point (verified working)"
    - path: ".git/refs/tags/phase-c"
      provides: "Annotated git tag marking Phase C completion"
  key_links:
    - from: "git tag phase-c"
      to: "github.com/omerblecher/VideoStreamAnalysis"
      via: "git push --tags"
      pattern: "phase-c"
---

<objective>
Verify the complete graceful shutdown flow end-to-end, then commit, tag phase-c, and push to GitHub to complete the assignment.

Purpose: Human confirmation that all three processes exit automatically on video end with no zombies, followed by the final git delivery required by assignment Phase C.

Output: Verified working shutdown, annotated git tag phase-c pushed to GitHub.
</objective>

<execution_context>
@C:/Users/omerb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omerb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-shutdown/03-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify graceful shutdown end-to-end</name>
  <files></files>
  <action>
    Human verifies the shutdown flow. No automated action — this task pauses for user confirmation.
  </action>
  <what-built>
    Graceful shutdown: multiprocessing.Event wired through all three processes, Streamer sets it on EOF, main.py joins with 5-second timeout and force-terminates stuck processes via try/finally.
  </what-built>
  <how-to-verify>
    1. Run the pipeline:
         python main.py &lt;your_video_file&gt;
    2. Watch the video play to completion — the window should close automatically when the last frame is displayed.
    3. Confirm that the terminal returns to a prompt on its own (no hanging).
    4. Optional — check no zombie processes:
         On Windows: Open Task Manager and confirm no python.exe processes remain after the terminal returns.
         On Linux/macOS: run `ps aux | grep python` after exit — should show no pipeline processes.
    5. Run again and press Ctrl+C mid-playback — confirm the pipeline exits (may not be graceful but should not hang indefinitely).

    Expected: Video plays, window closes, prompt returns, no leftover python processes.
  </how-to-verify>
  <verify>Human types "approved" to resume.</verify>
  <done>User confirms pipeline exits automatically after last frame with no zombie processes.</done>
  <resume-signal>Type "approved" if shutdown works correctly, or describe any issues (e.g., "hangs after last frame", "window stays open")</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Commit, tag phase-c, push to GitHub</name>
  <files>main.py, streamer.py, detector.py, viewer.py</files>
  <action>
    Commit all modified files (main.py, streamer.py, detector.py, viewer.py), create annotated tag phase-c, and push both to GitHub.

    Steps:
    1. Stage modified files:
         git add main.py streamer.py detector.py viewer.py

    2. Commit with a message that explains the shutdown design decision (per GIT-03):
         git commit -m "feat: Phase C — graceful shutdown via multiprocessing.Event

    All three pipeline processes now terminate automatically when the video ends.
    Shutdown design: Streamer sets a multiprocessing.Event after putting EOS_SENTINEL
    on the queue; main.py joins each process with a 5-second timeout, force-terminates
    any stuck process via terminate(), and wraps the join loop in try/finally to drain
    any unreleased SharedMemory signals. EOS propagation through Queues was already
    correct from Phase A; this adds the timeout guard and coordinated stop signal."

    3. Create annotated tag:
         git tag -a phase-c -m "Phase C complete: graceful shutdown propagation verified"

    4. Push commits and tag:
         git push origin master
         git push origin phase-c
  </action>
  <verify>
    git log --oneline -3
    git tag -l "phase-c"
    git ls-remote --tags origin phase-c
  </verify>
  <done>
    git log shows the Phase C commit at HEAD.
    git tag -l shows phase-c.
    git ls-remote confirms phase-c tag exists on origin (GitHub).
  </done>
</task>

</tasks>

<verification>
git log --oneline -5
git show phase-c --stat
git ls-remote --tags origin | grep phase-c
</verification>

<success_criteria>
- Human confirmed: pipeline exits automatically after last frame, no zombie processes
- git log shows Phase C commit at HEAD on master
- Annotated tag phase-c exists locally and on origin
- All four source files (main.py, streamer.py, detector.py, viewer.py) included in commit
</success_criteria>

<output>
After completion, create `.planning/phases/03-shutdown/03-02-SUMMARY.md` following the summary template.
</output>
